package com.swipeclean.app.ui.home

/**
 * Accesos rápidos de la fila de chips de la Home. La traducción a un filtro de
 * consulta concreto ocurre al navegar (ver `SwipeCleanNavHost`), no aquí: este
 * tipo es puramente de presentación.
 */
enum class HomeChip {
    SCREENSHOTS,
    HEAVIEST_PHOTOS,
    VIDEOS,
}
